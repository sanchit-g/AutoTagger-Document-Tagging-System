# Flask Configuration
FLASK_ENV=development
FLASK_HOST=0.0.0.0
FLASK_PORT=5000

# Security
SECRET_KEY=dev-secret-key-change-in-production

# Logging
LOG_LEVEL=INFO

# Database (optional - defaults to SQLite in project root)
# DATABASE_URL=sqlite:///autotagger.db